# Accessibility evidence

## What was done
- Added Semantics labels to key interactive elements (Saved parks button, Save/Unsave buttons, Use my location).
- Used Flutter's semantics debugger overlay to verify elements expose roles/labels.

## Evidence
Screenshot:
- docs/screenshots/accessibility_semantics.png
